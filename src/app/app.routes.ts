import { Routes } from '@angular/router';
import { LocalWeatherPageComponent } from './local-weather-page/local-weather-page.component';

export const routes: Routes = [
  { path: '', component: LocalWeatherPageComponent}
];
